import React from "react";
import { View, Text, StyleSheet } from "react-native";

const MapScreen = props => {
  return <Text>Pantalla de la mapa</Text>;
};

const styles = StyleSheet.create({});

export default MapScreen;
